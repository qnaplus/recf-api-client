{
  // Type-checks the scripts folder without emitting any build output.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": [
        "node"
    ],
  },
  "include": ["scripts"]
}
